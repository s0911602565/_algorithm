for (int i = 0; i < arr.length - 1; i++) {           // 回合數
    for (int j = 0; j < arr.length - 1 - i; j++) {   // 減 i，就是縮小比較範圍
        if (arr[j] > arr[j + 1]) {
            int temp = arr[j];
            arr[j] = arr[j + 1];
            arr[j + 1] = temp;
        }
    }
}